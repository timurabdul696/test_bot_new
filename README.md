# 🛒 Telegram shop bot (aiogram)

Телеграм-бот для магазина с разделением прав доступа (Админ/Пользователь), системой категорий, интерактивной корзиной и техподдержкой.

## 📖 Описание проекта
Бот представляет собой платформу для электронной коммерции, позволяющую автоматизировать продажи в Telegram

## 🛠 Технологический стек
* **Язык**: Python 
* **Библиотека**: `aiogram`
* **База данных**: `SQLite` (через кастомный `DatabaseManager`)
* **Состояния**: `FSM (MemoryStorage)`

## 📂 Структура проекта
Исходя из архитектуры репозитория:
* `data/` — конфигурация (`config.py`) и файл базы данных
* `filters/` — кастомные фильтры (`IsAdmin`, `IsUser`) для разграничения прав доступа
* `handlers/` — логика обработки команд и сообщений (меню, каталог, корзина, SOS)
* `keyboards/` — генерация Reply и Inline клавиатур
* `states/` — состояния (FSM) для оформления заказов и тикетов
* `utils/` — вспомогательные инструменты и менеджер б/д
* `loader.py` — инициализация бота, диспетчера и б/д
* `app.py` — точка входа 

## ✨ Основной функционал

### 👤 Для пользователя
* **Интерактивный каталог**: Навигация по категориям и просмотр карточек товаров с фото и описанием
* **Умная корзина**: Добавление товаров, изменение их количества и удаление прямо в чате через кнопки
* **Оформление заказа (FSM)**: Пошаговый сбор данных (имя, адрес) с возможностью подтверждения или возврата на предыдущий шаг
* **Статус заказа**: Просмотр текущего состояния активных покупок
* **Система SOS**: Возможность отправить запрос администрации и получить ответ

### 👑 Для администратора
* **Управление ролями**: Переключение в админ-режим через стартовое меню
* **Панель управления**: Специальное меню для работы с настройками каталога, заказами и вопросами пользователей

## 🔐 Безопасность
Доступ к функционалу строго разграничен с помощью `BoundFilter`:
* `IsAdmin`: Проверяет наличие ID пользователя в списке администраторов
* `IsUser`: Гарантирует доступ к клиентской части только для обычных пользователей